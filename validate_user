def validate_user(user_data:dict)->bool:
    
    
    age = user_data.get("age")
       # Если хоть одно условие НЕ ВЫПОЛНЕНО — сразу выходим
    return all([ user_data.get("name"),
                 user_data.get("email") and  "@" in user_data.get("email") and  "." in user_data.get("email"),
                 user_data.get("age") and 18<=user_data.get("age")<=100
        ])

  
   

    # Если мы дошли сюда — всё супер
   
user_data={
    "name": "Bob",
    "age": 22,
    "email":"roman.nasyrov@gmail.com"
    }
    
print(validate_user(user_data))

    

